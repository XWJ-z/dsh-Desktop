# 任务管理（TASKS.md）

> 项目看板：待办 / 进行中 / 已完成。更新规则：任务状态变化时同步更新本文件，并在
> `docs/DEVELOPMENT_LOG.md` 追加一条日志。

## 待办（Backlog）

| 编号 | 任务 | 优先级 | 备注 |
| --- | --- | --- | --- |
| T-013 | 自动更新 / 版本检查（壳版本 + DSH 版本） | P3 | 后期迭代；DSH 版本已可通过 config.json 手动升级 |
| T-014 | 打包产物体积优化（排除无用模块） | P3 | 安装包已降至约 98 MB；DSH 依赖树不再内置 |
| T-015 | 首次运行引导（profile 初始化检测与提示） | P2 | 新机器上 `$DSH_HOME/profiles` 可能未初始化 |
| T-021 | 打包流程末尾增加"愈合共享链接"步骤 | P2 | 规避 overwrite 重建 dist 后首启偶发解析失败 |
| T-023 | 安装程序代码签名（Authenticode） | P3 | **暂缓**（2026-08-14 老大决定"要钱算了，以后再搞"）。方案已定为 Azure Trusted Signing，指南见 `docs/T023-Azure-Trusted-Signing-指南.md`；electron-builder.yml 已留好 azureSignOptions 占位，开通后填值即可启用 |
| T-024 | 原生目录对话框修复（DSH 上游 worker 协议） | P3 | 纯套壳后不再做本地下游覆盖；如遇问题按 T-025 方案临时规避 |
| T-029 | **日志时间 + 启动界面优化（v0.4.2）**：本地时间戳、loading 版本行、下载进度、视觉细节 | P2 | 见 `代码审查/修复任务清单-v0.4.2-日志与启动界面-20260814.md`；实施完成，待 26 终审与实机回归 |

## 进行中（In Progress）

无。

## 已完成（Done）

| 编号 | 任务 | 完成时间 | 验证方式 |
| --- | --- | --- | --- |
| T-001 | 调研 DSH 启动方式与默认端口（3080） | 2026-08-14 | 阅读 `dsh` CLI 源码与 `dsh-web-app/cordis.patch.yml` |
| T-002 | 确定技术选型：Electron（环境无 Rust，Tauri 不可行） | 2026-08-14 | 环境探测（node/npm 可用，rustc 缺失） |
| T-003 | 搭建 Electron 项目骨架（package.json/main/preload/loading） | 2026-08-14 | 文件落盘 |
| T-004 | 生成应用图标（png + ico） | 2026-08-14 | 占位图标生成成功；后续 T-019 换为官方鲸鱼 |
| T-005 | DSH 入口定位逻辑（本地 → npx 缓存 → PATH） | 2026-08-14 | 代码审查（v0.3.0 起改为 npm 运行时管理） |
| T-006 | 端口自动顺延 + 服务就绪探测 | 2026-08-14 | 打包版验证：3080 被占用自动用 3081 |
| T-007 | 编写项目管理文档（PROJECT/TASKS/MILESTONES） | 2026-08-14 | 文档落盘 |
| T-008 | 编写开发日志文档 | 2026-08-14 | 文档落盘 |
| T-009 | npm 安装依赖（electron / 打包工具） | 2026-08-14 | 安装成功（工作区内置缓存规避 EPERM） |
| T-010 | 开发模式启动验证：桌面窗口加载 DSH Web GUI | 2026-08-14 | 窗口枚举 `你好 — DeepSeek Harness` + 日志 `加载完成` |
| T-011 | 制作 NSIS 安装器（electron-builder，`npm run installer`） | 2026-08-14 | Setup EXE 生成；静默安装 → 注册表/快捷方式/卸载入口验证 |
| T-012 | 原生模块 ABI：确认无需 rebuild（N-API 预编译） | 2026-08-14 | ABI 预检通过；electron-as-node 实测加载 |
| T-016 | 打包模式运行时打通（electron-as-node + --expose-internals） | 2026-08-14 | 打包版启动 dsh → HTTP 200 |
| T-017 | 正式打包命令 `npm run pack`（@electron/packager，asar 关闭） | 2026-08-14 | 打包产物完整验收（启动/GUI/退出） |
| T-018 | 优雅退出与日志落盘验证 | 2026-08-14 | 日志链完整 + 进程/端口清理 |
| T-019 | 品牌改造：DeepSeek 官方鲸鱼图标 + 名称 DSH-Desktop | 2026-08-14 | 图标栅格化成功；名称统一（productName/窗口/exe/菜单） |
| T-020 | 目录重组：开发文件移入 `app/`，开发日志移入 `docs/dev-log/` | 2026-08-14 | 移动完成；文档路径同步更新 |
| T-022 | 安装器依赖树修复：afterPack 钩子补齐 peer 依赖 | 2026-08-14 | 内置 DSH 时代的依赖树修复（v0.3.0 纯套壳后不再需要，已移除） |
| T-025 | 目录选择器固定 browse 后端（overlay + `--patch`） | 2026-08-14 | `dsh --dump-config --patch` 验证（v0.3.0 纯套壳后已移除 overlay） |
| T-026 | **纯套壳架构（v0.3.0）**：DSH 不内置，壳自带 npm 按版本拉取 | 2026-08-14 | 隔离环境验证：内置 npm → install 528 包 → `dsh --version` 正常；安装包 98 MB |
| T-027 | **代码审查修复（v0.3.0，6 项）**：H1 logo 内联、H2 窗口分离+加载过渡、H3 内置真实 Node、M1 子进程清理、M2 崩溃提示、M3 日志定向 | 2026-08-14 | 见 `代码审查/修复任务清单-20260814.md`；安装包 120.7 MB（含 Node 74MB） |
| T-028 | **菜单优化 + DSH 版本检查（v0.4.0）**：菜单去重/分组/补入口；检查更新/一键升级/启动静默检查/关于集成 | 2026-08-14 | 见 `代码审查/修复任务清单-v0.4-菜单与版本检查-20260814.md`；compareSemver 全用例通过、真实 registry 查询验证；打包产物重建完成，待 26 终审 |
| T-029 | **日志时间 + 启动界面优化（v0.4.2）**：本地时间戳修复 UTC+8、loading 版本行/下载进度/日志折叠/就绪✓/入场动画/首次文案 | 2026-08-14 | 见 `代码审查/修复任务清单-v0.4.2-日志与启动界面-20260814.md`；localTimestamp/dirSizeMB 逻辑验证通过、开发模式日志时间确认；打包重建中，待 26 终审 |

## 里程碑关联

- **M1 可运行桌面壳**（2026-08-14 ✅）：T-001 ~ T-008、T-010
- **M2 打包分发**（2026-08-14 ✅）：T-009、T-012、T-016 ~ T-018
- **M3 体验打磨**（2026-08-14 🔄）：T-011、T-022、T-025 ~ T-027 完成；T-013 ~ T-015、T-021、T-023、T-024 待办
- **M3.1 菜单与版本检查（v0.4.0）**：T-028 完成（待 26 终审）；T-013 部分落地（DSH 版本检查，壳版本检查仍待办）
- **M3.2 日志与启动界面（v0.4.2）**：T-029 完成（待 26 终审）
