# DSH-Desktop 修复任务清单 v0.4（6 执行）

> 来源：26 技术总监审查报告 v3.0（2026-08-14）——菜单优化 + DSH 版本检查
> 详细方案见：`代码审查/审查报告-v3.0-菜单与版本检查方案-20260814.md`
> 前置状态：H1~H4、M1~M3、L4~L6 均已修复 ✅（v2.0/v3.0 验收通过）
> 本清单 = 新需求：菜单优化（任务A）+ DSH 官方版本检查（任务B）

---

## 任务A：菜单优化（约30分钟）

**目标：** 重构 `buildMenu()`（main.js:636-683），去重、分组、补入口。

**改动内容：**

```
📁 文件
 ├─ 重新加载界面        CmdOrCtrl+R    （保留）
 ├─ 打开日志目录        （保留，从帮助移过来）
 ├─ 打开数据目录        ★新增：shell.openPath(app.getPath('userData'))
 ├─ ────────
 └─ 退出

👁 视图
 ├─ 实际大小 / 放大 / 缩小    （保留）
 ├─ ────────
 ├─ 全屏                     （保留）
 └─ 开发者工具          F12   （★从文件菜单移来，只保留这一处）

❓ 帮助
 ├─ 检查 DSH 更新       ★新增（任务B实现，先留占位）
 ├─ ────────
 ├─ 关于 DSH-Desktop          （保留，升级为含版本检查按钮，见任务B）
 ├─ ────────
 ├─ DeepSeek 官网       ★新增：shell.openExternal('https://www.deepseek.com')
 └─ DSH 项目主页        ★新增：shell.openExternal('https://github.com/deepseek-ai/dsh')
```

**要点：**
1. "重新加载界面"和"开发者工具"**各只保留一处**（文件→重新加载；视图→开发者工具）
2. "打开日志目录"移到【文件】菜单（用户操作归文件，信息归帮助）
3. 新增"打开数据目录"——用户排查问题用（DSH 装在 %APPDATA%\DSH-Desktop\dshenv）
4. 官方链接用 `shell.openExternal`（HTTPS 白名单已兼容）

**验证：** 应用菜单：无重复项；点击各项功能正常（日志目录/数据目录/官网/项目主页）

---

## 任务B：DSH 官方版本检查（约1-2小时）

**目标：** 检查 npm 官方包 `@deepseek-ai/dsh` 最新版本（dist-tags.latest），与当前配置/已安装版本对比，提示升级。

### B1. main.js 新增查询函数（核心）

```js
const https = require('node:https');   // 文件顶部已有 http/net，补 https

/** 查询 npm registry 上 DSH 最新版本；失败/超时返回 null（静默） */
function fetchLatestDshVersion() {
  return new Promise((resolve) => {
    const cfg = readShellConfig();
    const pkgPath = cfg.dshPackage.replace('/', '%2f'); // scoped 包需编码 /
    const base = (cfg.registry || 'https://registry.npmmirror.com').replace(/\/$/, '');
    const req = https.get(`${base}/${pkgPath}`, { timeout: 8000 }, (res) => {
      let body = '';
      res.on('data', (c) => (body += c));
      res.on('end', () => {
        try { resolve(JSON.parse(body)['dist-tags']?.latest ?? null); }
        catch { resolve(null); }
      });
    });
    req.on('error', () => resolve(null));
    req.on('timeout', () => { req.destroy(); resolve(null); });
  });
}

/** 语义化比较，支持 -rc.x 预发布：正式版 > 预发布版；返回 1/0/-1 */
function compareSemver(a, b) {
  const pa = String(a).split('-'), pb = String(b).split('-');
  const va = pa[0].split('.').map(Number), vb = pb[0].split('.').map(Number);
  for (let i = 0; i < 3; i++) {
    const x = va[i] || 0, y = vb[i] || 0;
    if (x !== y) return x > y ? 1 : -1;
  }
  const ra = pa[1] ? 0 : 1, rb = pb[1] ? 0 : 1; // 无预发布号视为正式版
  if (ra !== rb) return ra > rb ? 1 : -1;
  return 0;
}
```

### B2. 升级动作（改写 config.json）

```js
/** 备份并改写 config.json 的 dshVersion；成功返回 true */
function updateDshVersion(newVersion) {
  const file = path.join(app.getAppPath(), 'config.json');
  try {
    fs.copyFileSync(file, `${file}.bak`);          // 先备份
    const cfg = JSON.parse(fs.readFileSync(file, 'utf8'));
    cfg.dshVersion = newVersion;
    fs.writeFileSync(file, JSON.stringify(cfg, null, 2), 'utf8');
    return true;
  } catch { return false; }
}
```

### B3. 检查更新弹窗（菜单"检查 DSH 更新"点击）

```js
/** 菜单项：帮助 → 检查 DSH 更新 */
async function checkDshUpdate(win) {
  const cfg = readShellConfig();
  const current = installedDshVersion() ?? cfg.dshVersion;
  const latest = await fetchLatestDshVersion();
  if (!latest) {
    dialog.showMessageBox(win, { type: 'info', title: '检查更新',
      message: '无法连接 npm 源', detail: '请检查网络后重试。' });
    return;
  }
  const cmp = compareSemver(current, latest);
  if (cmp >= 0) {
    dialog.showMessageBox(win, { type: 'info', title: '检查更新',
      message: '已是最新版本', detail: `DSH：${current}` });
    return;
  }
  const r = dialog.showMessageBoxSync(win, {
    type: 'question', title: '发现新版本',
    message: `DSH 有新版本：${current} → ${latest}`,
    detail: '升级后将修改 app/config.json 的 dshVersion，重启应用自动安装新版。\n是否立即升级？',
    buttons: ['立即升级', '稍后'], cancelId: 1,
  });
  if (r === 0) {
    if (updateDshVersion(latest)) {
      dialog.showMessageBoxSync(win, { type: 'info', title: '升级',
        message: '配置已更新', detail: '应用将重启以安装 DSH 新版本。' });
      app.relaunch(); app.exit(0);
    } else {
      dialog.showMessageBoxSync(win, { type: 'error', title: '升级',
        message: '改写 config.json 失败', detail: '请手动修改 dshVersion 后重启。' });
    }
  }
}
```

### B4. 启动后静默检查 + 菜单提示后缀

```js
// 服务就绪后（main.js:723 附近）异步触发：
let dshHasUpdate = false;
fetchLatestDshVersion().then((latest) => {
  const current = installedDshVersion() ?? readShellConfig().dshVersion;
  if (latest && compareSemver(current, latest) < 0) {
    dshHasUpdate = true;
    appendLog('info', `DSH 有新版本：${current} → ${latest}（菜单"帮助 → 检查 DSH 更新"）`);
    Menu.setApplicationMenu(buildMenu()); // 重建菜单刷新提示后缀
  }
});

// buildMenu() 中"检查 DSH 更新"项 label 动态化：
label: `检查 DSH 更新${dshHasUpdate ? '（有新版本）' : ''}`
```

### B5. 关于对话框集成（可选增强）

在"关于 DSH-Desktop" detail 中追加最新版本行 + "检查更新"按钮：
```js
const latest = await fetchLatestDshVersion();
detail: `版本：${app.getVersion()}\nDSH：${cfg.dshPackage}@${installedDshVersion() ?? cfg.dshVersion}\nDSH 最新：${latest ?? '未知'}\n服务地址：${webUrl()}`
// 或 buttons: ['检查更新', '关闭'] 复用 checkDshUpdate()
```

---

## 任务B 边界与注意

| 项 | 约定 |
|----|------|
| 静默失败 | 断网/源不可达：仅日志，菜单无提示后缀，不打扰 |
| registry 一致性 | 查询用 config.json 的 registry（npmmirror），与安装源一致 |
| 防抖 | 手动检查弹窗前加 10 秒冷却（记录 lastCheckAt，防止连点）|
| 备份 | updateDshVersion 先 copyFileSync 到 config.json.bak |
| 不改 DSH | 全程只动壳（main.js/preload），DSH 本体零接触 |
| 失败兜底 | 升级失败提示手动改 config.json，应用不崩 |

---

## 回归验证清单（Windows）

**任务A：**
- [ ] 菜单无重复项（重新加载/开发者工具各一处）
- [ ] 打开日志目录、打开数据目录正常打开对应文件夹
- [ ] DeepSeek 官网、DSH 项目主页用系统浏览器打开

**任务B：**
- [ ] 联网点"检查 DSH 更新"：显示 当前 vs 最新 版本
- [ ] 有新版点【立即升级】：config.json 备份+改写 → 应用重启 → 自动安装新版 → 关于里版本更新
- [ ] 无新版：显示"已是最新版本"
- [ ] 断网点检查：友好提示，应用不卡不崩
- [ ] 启动后有新版：菜单项出现"（有新版本）"后缀
- [ ] 升级失败场景：提示手动修改，config.json.bak 存在

**既有功能回归：**
- [ ] 启动流程正常（loading 阶段①→④、日志滚动）
- [ ] 选择工作区正常
- [ ] 关闭应用无残留 node/npm 进程

---

## 完成标准

任务A + 任务B 全部实现，回归清单全绿 → 通知 26 终审。
